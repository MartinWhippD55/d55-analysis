# Playwright MCP artifacts
.playwright-mcp/

# VS Code settings
.vscode/

# Reference repos (local copies for context only)
reference-repos/

# Derived client delivery bundles (regenerable via client_bundle)
programmes/*-client-bundle/

# Generated deliverables (regenerable via spec-to-deliverables regenerate_all.py)
deliverables/

# MCP secrets — never commit API tokens
.kiro/settings/*.env
