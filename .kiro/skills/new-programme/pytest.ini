[pytest]
testpaths = tests
markers =
    pdf: tests that render a PDF via Playwright/Chromium (skipped if unavailable)
    browser: tests that drive a headless browser via Playwright/Chromium (skipped if unavailable)
