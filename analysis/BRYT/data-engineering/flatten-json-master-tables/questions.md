# Questions — Flatten JSON Master-Record Tables

## Open

- Which specific master-record tables contain JSON columns?
- Is the JSON schema consistent across rows, or does it vary?
- Are there nested objects / arrays within the JSON that need special handling?
- Who are the downstream consumers of these tables?
- Can we version the flattened schema, or do consumers need a stable contract?
- Should we maintain the original JSON column alongside flattened columns during transition?
- What layer do these tables sit in today (raw / refined / curated)?

## Answered

_(none yet)_
